<resources>
    <!-- Base application theme. -->
    <style name="Theme.TaxiLog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow_3</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorContainer">@color/gray_800</item>
        <item name="colorOnContainer">@color/yellow_3</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow_3</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSecondaryContainer">@android:color/transparent</item>
        <item name="colorOnSecondaryContainer">@color/yellow_3</item>

        <!-- Tertiary brand color (Neutralized) -->
        <item name="colorTertiary">@color/gray_700</item>
        <item name="colorOnTertiary">@color/white</item>

        <!-- Background & Surface -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="colorSurface">@color/dark_gray</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorSurfaceVariant">@color/gray_800</item>
        <item name="colorOnSurfaceVariant">@color/gray_300</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/hint_night</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="bottomNavigationBackground">@color/dark_gray</item>
        <item name="colorBackgroundFloating">@color/dark_gray</item>
    </style>
</resources>